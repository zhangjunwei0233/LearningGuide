---
title: 多项式回归
draft: "false"
tags:
  - 机器学习
---

## 一、回归任务

很多时候目标变量和特征参数的关系不是线性的，需要用多项式来拟合非线性关系

## 二、思路

根据Taylor展开的原理，可以通过多项式来进行回归。将特征变量的高次项看作是新的特征，问题就转化为[[多变量线性回归 (Linear Regression with multiple variables)|多变量线性回归]]

**例子**：对于单个特征x，可以构造：
$$
h_\theta(x) = \theta_0 + \theta_1 x + \theta_2 x^2 + \theta_3 x^3 + \dots + \theta_n x^n
$$

**多特征情况**：对于多个特征，可以包含：
- **多项式项**：$x_1^2, x_2^2, x_1^3, x_2^3, \dots$
- **交互项**：$x_1 x_2, x_1 x_3, x_2 x_3, \dots$
- **混合项**：$x_1^2 x_2, x_1 x_2^2, \dots$

## 三、优化

### 1. 特征缩放的重要性

多项式特征会产生很大的数值差异，特征缩放变得更加重要：
- $x_1 \in [1, 1000]$ → $x_1^2 \in [1, 1000000]$
- $x_1 \in [1, 1000]$ → $x_1^3 \in [1, 1000000000]$

### 2. 正则化

高次多项式容易过拟合，通常需要使用正则化：
- **岭回归**：适用于保留所有特征
- **Lasso回归**：可以自动进行特征选择

### 3. 选择多项式次数

- 使用交叉验证选择最优的多项式次数
- 观察学习曲线判断是否过拟合或欠拟合
- 一般不超过3-4次，除非有充分的理论依据